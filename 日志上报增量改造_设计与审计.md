# 日志上报体系 — 增量改造设计 & 影响面审计

> 核心约束:**纯增量、零破坏**。`log_manager_plugin` 与 CLS 上报层是 google/space/tesla 三端共用 + 现网在跑,本次改造**只增不改**现有公共契约;任何动到共用逻辑的改动必须三端同步并通过审计清单。

---

## 0. 目标

1. 把异常/事件上报队列(现 `LogQueueManager`)做成**可靠队列**(重试/落盘/看返回码/三触发批量/内存上限),修掉"失败即丢"。
2. 支持**多 topic 通道**,topic 由外部配置注入。
3. 预留**接口日志(api_logs)新 topic**:后台开关驱动 + 自动分包上传。
4. 全程不破坏现有 `LogManager` / `LogReporter` / `LogManagerInterceptor` 公共 API。

## 1. 现状审计

### 1.1 三种日志模式
| 模式 | 数据形态 | 触发 | topic | 现状 |
|---|---|---|---|---|
| A 实时事件流 | 单条 KV | 自动(出事即报) | domain_logs / netstat_logs | `LogQueueManager`(弱) |
| B 文件诊断包 | 整包 zip | 用户/排障手动 | (回调) | `LogReporter.uploadLogs` ✅ |
| C 文件分包·按需 ⭐ | 大块文本分包 | **后台开关驱动** | **api_logs(待加)** | 分包✅ / 开关驱动❌ |

### 1.2 三端平行实现(改逻辑必须同步)
| 组件 | google | space | tesla | 同步? |
|---|---|---|---|---|
| LogService / LogQueueManager / AppLog | `google_core/.../util/log_manager/` | `space_core/.../modules/logManager/` | `tesla_core/.../modules/logManager/` | **必须三端同步** |
| UnifiedLogManager(分类上报) | ✅ 有 | ❌ 无 | ❌ 无 | 仅 google |
| topic 来源 | `LinesConfig.getLogServerLogTopicId/NetTopicId` | 同名(各一份) | 同名(各一份) | 值各端配置 |

> ⚠️ 既存不一致:space 的 `LogQueueManager.enqueue` 每次重置 timer;google/tesla 只在 `_sendTimer==null` 时设。本次顺带统一(见 §5)。

### 1.3 底层 LogProducer(tencentcloud_cls_sdk_dart_producer)
- **纯 Dart HTTP 上传**(非 native SDK):protobuf 序列化 + 腾讯云签名(HMAC-SHA1,**用 secretKey**)+ zstd 压缩 + `http.post /structuredlog`。
- **零可靠性**:一次性 POST,无 buffer/重试/落盘。返回精细错误码(`ok/sendNetworkError/sendQuotaError(429)/sendUnauthorized/...`)但**上层 `LogQueueManager` 一个都没用**。
- 🔴 **安全**:secretId/secretKey 来自 `GoogleRuntimeConfig.logSecretId/Key`(编译期常量)→ **长期密钥进二进制**,可被逆向提取(混淆缓解不了,运行时要明文 HMAC)。本次改造建议顺带换 STS 临时密钥 / 服务端代理(代码已支持 `accessToken`→`X-Cls-Token`,改动小)。

## 2. 🔴 不可改红线(改了破坏三端,审计必查)

| 契约 | 破坏范围 |
|---|---|
| `LogManager.d/i/w/e/v/f` / `init` / `updateConfig` | 三端 400+ 调用 |
| `LogManagerConfig` 现有字段(可加 optional,**禁删/改名/改类型**) | 三端启动初始化 |
| `LogManagerInterceptor` 构造签名 | 各处 dio 初始化 |
| `LogUploadCallback` / `LogUploadStringCallback` typedef | 三端上报回调实现 |
| `LogBatch` 字段 / `toJson` | 上报序列化 |
| `LogReporter.uploadLogs*` 签名/返回 | 业务上报调用 |
| `LogService` / `LogQueueManager.enqueue` / `AppLog` 现有签名 | 三端平行使用 |

**原则**:以上只读不改。新增一律走新文件/新类/新方法(可选参数 + 默认值)。

## 3. ✅ 纯增量设计

### 3.1 新增(log_manager_plugin,新文件,零腾讯依赖)
```dart
// lib/src/reporting/log_sink.dart —— 实际发送抽象,CLS 由外部注入
abstract class LogSink {
  Future<bool> send(String topic, List<LogEvent> batch); // 返回成功与否,队列据此重试
}

// lib/src/reporting/topic_channel.dart
enum TopicMode { realtime, chunkedFile }   // 实时事件流 / 文件分包
class TopicChannel {
  final String topic;        // 外部配置,如 'api_logs'
  final TopicMode mode;
  final LogSink sink;        // 外部注入
  bool enabled;              // 后台远程开关(默认 false)
  int maxBatchBytes;         // 分包大小,后台可下发
  LogScope? scope;           // 捞哪些:用户/日期/url 匹配
}

// lib/src/reporting/event_reporter.dart —— 可靠队列(修掉所有现状 bug)
class EventReporter {
  void registerTopic(TopicChannel ch);          // 注册 topic 通道
  void enqueue(String topic, LogEvent e);        // 模式 A:实时事件
  Future<void> applyRemoteConfig(Map cfg);       // 后台驱动:更新 enabled/batchSize/scope + 触发 C 类分包
  Future<void> flush();                          // 真 await,退出兜底
  Future<void> dispose();
}
```
EventReporter 内部解决:数量+字节+时间**三触发**批量、失败**重试 + 落盘**(下次启动补传)、看 `LogProducerResult` 返回码(429 退避)、`_buffer` **大小上限**、`dispose` 资源回收。

### 3.2 远程全量日志开关(full_logs topic)—— 比"只传接口日志"更优
后台开关开 → 把该设备**全量日志**(控制台看到的所有 d/i/w/e/v/f,不只接口)上报,= 远程捞日志排障能力。

**接入点(纯增量,已确认)**:`LogManager` 的 `_CustomMultiOutput.output()`(log_manager.dart:188)是所有日志(控制台+文件)的唯一汇聚点。在它末尾加第三分支 → tap 出全量日志行 → `full_logs` 队列。`_CustomMultiOutput` 是私有类,`LogManagerConfig` 加 optional 字段 + `LogManager.remoteLogTap` 可选注入,**不碰任何公共签名**。

**两种模式(配合用)**:
- 历史回捞:开关开瞬间,本地已存日志分包传(复用 `LogReporter.uploadLogsAsString`)——看"之前";
- 实时转发:之后新日志经 tap 实时上报——看"接下来"。

**🔴 全量必须守的产品约束**(否则 CLS 成本/隐私出事):
1. **定向 + 自动过期**:只对指定 user/device 开,开 N 小时自动关,严禁全局长开;
2. **脱敏**:全量含 token/手机号/订单,上报前脱敏;
3. **等级阈值**:开关带 `minLevel`(默认 warning,控量);
4. **限流退避**:靠 §5.5-1 的重试/429 退避兜住,别打爆 CLS。

> 数据源不再依赖 `LogManagerInterceptor`(那只是接口日志),而是 `LogManager` 全量出口——覆盖面更大、复用度更高。命名用 `full_logs`/`remote_logs`,非 `api_logs`。

### 3.3 CLS sink(主项目侧,薄适配,插件零腾讯依赖)
```dart
class ClsLogSink implements LogSink {           // 留在 *_core,不进插件
  Future<bool> send(String topic, List<LogEvent> b) async =>
    (await producer.send(b.map((e)=>e.toClsLog()).toList())) == LogProducerResult.ok;
}
```
注册(topic 外部配置):`reporter..registerTopic(TopicChannel('api_logs', chunkedFile, ClsLogSink(apiProducer)))`。

### 3.4 与 network_line_manager 合流
`network_line_manager` 的 `TelemetryPort` 直接以 `EventReporter` 为实现 → 线路遥测和异常上报走同一条可靠管道,**后台下行配置(开关/灰度)复用同一通道**。

## 4. 现有代码怎么接(向后兼容)
- google 的 `UnifiedLogManager.reportXxx` / `LogService.uploadXxx` **签名不变**,内部改为转调 `EventReporter`(老调用方无感)。
- 灰度开关:`EventReporter` 未注册时,保留走旧 `LogQueueManager` 路径,可随时回退。

## 5. 三端同步规则
1. CLS 上报层(LogService/LogQueueManager/AppLog)若改逻辑 → **google + space + tesla 同时改**,且顺带统一 timer 策略(以 google/tesla 的"仅 null 时设"为准,或显式定义)。
2. `LogServiceConfig` / `LinesConfig` 新增 topic 字段 → 三端 `getLogServerXxxTopicId` + 初始化处同步赋值。
3. `UnifiedLogManager` 仅 google;space/tesla 若要分类上报能力,从插件层(EventReporter)共享,不各写一份。
4. 新增能力优先放 `log_manager_plugin`(三端复用),而非各 core 重复。

## 5.5 推荐路线:产品优先的最小增强(先做这个)

§3 的 EventReporter 下沉是**长期完备解**(三端复用 + 与 network_line_manager 合流),但产品当前不需要全套。**优先在现有 `LogQueueManager`/`LogService` 上最小增强**,拿到全部产品收益、风险最低、不新建架构:

| 优先级 | 改动 | 范围 | 收益 |
|---|---|---|---|
| **1** | `LogQueueManager._flush` 改 async:看 `LogProducerResult` 返回码 + 失败保留重试(429 退避)+ `maxBufferSize` 内存上限 + `flushNow` 改 `Future`(退出可 await) | 三端同步(google/space/tesla 各一份) | 修"失败即丢",排障数据不丢(覆盖网络抖动/CLS短故障) |
| **2** | 远程全量日志开关(`full_logs` topic):`LogService.registerTopic/queueOf` 新增方法(不动 logQueue/demoQueue);`LogManager._CustomMultiOutput` 加 tap + `LogManagerConfig` 加 optional 开关字段;历史回捞复用 `uploadLogsAsString`;后台定向开关 + 自动过期 + 脱敏 + minLevel 控量 | google(+按需 space/tesla;LogManager 改动惠及三端) | 远程捞设备全量日志排障(产品新需求,比只传接口日志更优) |
| **3** | `reportLineSwitch`/`reportIMStatusChange` 取消注释 enqueue | google | 后台可见切线/IM 事件 |
| 缓 | 落盘补传(App 被杀/长断网)、EventReporter 下沉、secretKey→STS | — | 产品有更高要求时再上 |

> 取舍:第 1 步只做**内存重试**(覆盖 ~90% 抖动场景),**落盘补传**成本更高(文件IO+启动补传),不卡当前,列为下一步。
> 这条最小增强路线与 §3 完备架构**不冲突**:它是过渡,将来要下沉时 `EventReporter` 可包住增强后的 `LogQueueManager`。

## 5.6 沉淀公共日志接口 + 两阶段路线(已定方向)

把统一门面落在 `LogManager`(强化为公共日志接口),分两阶段:先单端验证、再全项目收敛。**先沉淀接口、再改上层**(避免改两次)。

### 公共日志接口(强化 LogManager,纯增量)
```dart
class LogManager {
  // 已有不动:d/i/w/e/v/f / init / updateConfig
  static void setReportSink(LogReportSink sink);                 // 注入上报实现(CLS 由主项目注入,插件零后端依赖)
  static void report(String topic, Map<String,String> fields, {Level level});  // 统一结构化上报入口
  static void enableRemoteFullLog({required bool enabled, Level minLevel});     // full_logs 远程全量开关
}
abstract class LogReportSink { Future<bool> send(String topic, List<LogEntry> batch); }
class LogEntry { final String topic; final Map<String,String> fields; final int level; final int timeMs; }
```
内部:`report`→可靠队列(三触发/重试/落盘/上限)→sink;`enableRemoteFullLog`→`_CustomMultiOutput` tap 全量日志进同一队列。

### 两阶段
| 阶段 | 做什么 | 范围 | 性质 |
|---|---|---|---|
| **P0** | 插件沉淀公共接口 + 可靠队列(默认不启用) | log_manager_plugin,纯新增 | 零破坏 |
| **P1** | google:`ClsLogSink` 注入 + `UnifiedLogManager`/`LogService` 内部转调(签名不变+回退开关)+ 修可靠性 + full_logs | google 单端 | 增量+回退 |
| 测试 | google 跑通(正常/弱网不丢/full_logs/老调用无感) | google | 验收 |
| **阶段二** | space/tesla 同步 + 各入口(LoggerUtils/各 *LogManagerUtils)收敛转调 + 崩溃三处(Sentry/Bugly/CLS)去重 | 三端全项目 | 收敛 |

> 阶段二动全项目前必须:阶段一 google 验证通过 + 崩溃监控以谁为准已拍板。

## 5.7 P0 审计结论与遗留(2026-05)

P0(公共接口)经 1 实现 + 3 维审计:**零破坏 ✅、analyze 0 error ✅**。可靠性/API 审计发现的 blocker/major 已修复:
- [已修·blocker] force 退出清 `_backoffUntil`(避免退避锁死丢数据)
- [已修·blocker] force 重试空批守卫(避免 `batch.first` 崩溃)
- [已修·blocker] full_logs 脱敏钩子 `setFullLogDesensitizer`(启用 full_logs **必须配**)
- [已修·major] `sink.send` 加 `sendTimeout`(退出兜底不被无响应后端卡住)
- [已修·major] dispose 用 `_closing` 先拒新入队再发残留(消除竞态丢失窗口)
- [已修·major] 三触发用 `_drainScheduled` 防重复 microtask,`_sending` 兜并发

遗留(后续阶段处理,非 P0 阻塞):
- **network_line_manager 合流**:`LogReportSink` 优先级/多 sink/路由 → P2 接 `TelemetryPort` 时再扩展(P0 单 sink 单 topic)。
- **space 三端 timer 策略**:P1 改三端 `LogQueueManager` 时统一为 `??=`(仅 null 时设),勿沿用 space 的逐次重置。
- **topic 常量集中管理**(LogTopic enum)→ 多 topic(api_logs/crash 等)落地时。
- 退避用 `DateTime` 非单调时钟(minor):移动端时钟回拨概率低,记为已知。
- 脱敏是 `LogReportSink`/宿主责任,插件只留钩子。

## 6. 落地步骤(渐进 / 可回退 / 可审计)
| 步 | 内容 | 风险 | 验证 |
|---|---|---|---|
| P0 | 插件新增 `LogSink`/`TopicChannel`/`EventReporter`(纯新文件,不导出冲突) | 极低 | 单测 + 不触碰现有文件 |
| P1 | google `UnifiedLogManager` 内部转调 `EventReporter`(签名不变,带回退开关) | 低 | 现网上报对比、可关开关回退 |
| P2 | 修可靠性:重试/落盘/返回码/三触发/上限,三端 LogQueueManager 对齐 | 中(三端同步) | 三端各跑 + 弱网/CLS 故障模拟 |
| P3 | 新增 `api_logs` topic + 后台开关驱动分包上传 | 中 | 后台开关灰度小流量 |
| P4(可选) | secretKey → STS 临时密钥 / 服务端代理 | 中 | 密钥不再入包验证 |

## 7. 审计验收清单(每步改完自查)
- [ ] 现有公共契约(§2)签名**零变更**(grep 调用点编译通过)
- [ ] `LogManagerConfig` 只增 optional 字段,无删除/改名/改类型
- [ ] 新增类全在新文件,`log_manager_plugin.dart` 导出只增不改
- [ ] 三端(google/space/tesla)CLS 上报层改动一致,无遗漏
- [ ] 旧上报路径保留 + 可回退开关,新路径默认不影响老调用方
- [ ] `flutter analyze` 三端 + 插件 0 error;现有单测全过
- [ ] 真机:正常网络上报正常;弱网/断网不丢(落盘补传);退出 flush 不丢
